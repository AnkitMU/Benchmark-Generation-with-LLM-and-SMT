-- Benchmark: IOTsensornetwork / balanced
-- Type: SAT Constraints Only
-- Generated: 2026-04-10T11:56:16.780755
-- Count: 66

-- #1 Collection Not Empty (Simple) in Device
-- Difficulty: easy
context Device
inv: self.tickets->notEmpty()

-- #2 Collection Maximum Size in IoTPlatform
-- Difficulty: medium
context IoTPlatform
inv: self.networks->size() <= 1

-- #3 Collection As Sequence Conversion in Cluster
-- Difficulty: medium
context Cluster
inv: self.edgeNodes->asSequence()->notEmpty()

-- #4 Attribute Not Null (Simple) in ActuatorNode
-- Difficulty: easy
context ActuatorNode
inv: self.actuatorType <> null

-- #5 Attribute Not Null (Simple) in AlertRule
-- Difficulty: easy
context AlertRule
inv: self.severity <> null

-- #6 Attribute Not Null (Simple) in EventLog
-- Difficulty: easy
context EventLog
inv: self.time <> null

-- #7 Collection Must Be Empty in User
-- Difficulty: easy
context User
inv: self.alerts->isEmpty()

-- #8 Association Exists in Role
-- Difficulty: easy
context Role
inv: self.platform <> null

-- #9 Association Exists in Alert
-- Difficulty: easy
context Alert
inv: self.rule <> null

-- #10 Association Exists in Gateway
-- Difficulty: easy
context Gateway
inv: self.cluster <> null

-- #11 Association Exists in CommunicationLink
-- Difficulty: easy
context CommunicationLink
inv: self.stream <> null

-- #12 Association Exists in Metric
-- Difficulty: easy
context Metric
inv: self.sensor <> null

-- #13 Association Exists in DataStream
-- Difficulty: easy
context DataStream
inv: self.link <> null

-- #14 Association Exists in OTAUpdate
-- Difficulty: easy
context OTAUpdate
inv: self.targetDevice <> null

-- #15 Association Exists in Location
-- Difficulty: easy
context Location
inv: self.device <> null

-- #16 Association Exists in APIKey
-- Difficulty: easy
context APIKey
inv: self.user <> null

-- #17 Numeric Is Odd in Battery
-- Difficulty: trivial
context Battery
inv: self.healthPct mod 2 = 1

-- #18 Collection Size in Range in Firmware
-- Difficulty: expert
context Firmware
inv: self.devices->size() >= 2 and self.devices->size() <= 2

-- #19 Numeric Is Even in SolarPanel
-- Difficulty: trivial
context SolarPanel
inv: self.wattPeak mod 2 = 0

-- #20 String Maximum Length in AlertRule
-- Difficulty: medium
context AlertRule
inv: self.ruleId.size() <= 41

-- #21 Collection Unique Attribute Values in OTAUpdate
-- Difficulty: easy
context OTAUpdate
inv: self.tickets->isUnique(x | x.status)

-- #22 SortedBy Operation in AccessPolicy
-- Difficulty: expert
context AccessPolicy
inv: self.roles->notEmpty() implies self.roles->sortedBy(x | x.roleName)->first().roleName <= self.roles->sortedBy(x | x.roleName)->last().roleName

-- #23 String Starts With in AccessPolicy
-- Difficulty: hard
context AccessPolicy
inv: self.description.size() >= 10 implies self.description.substring(1, 10) = 'value'

-- #24 SortedBy Operation in DataStream
-- Difficulty: expert
context DataStream
inv: self.alertRules->notEmpty() implies self.alertRules->sortedBy(x | x.condition)->first().condition <= self.alertRules->sortedBy(x | x.condition)->last().condition

-- #25 Boolean Guard in Alert
-- Difficulty: trivial
context Alert
inv: self.acknowledged

-- #26 String Minimum Length in Metric
-- Difficulty: medium
context Metric
inv: self.unit.size() >= 14

-- #27 String Contains Substring in MaintenanceTicket
-- Difficulty: medium
context MaintenanceTicket
inv: self.status.indexOf('value') >= 0

-- #28 Collect (Map) Operation in Cluster
-- Difficulty: hard
context Cluster
inv: self.edgeNodes->collect(x | x.cpuCores)->notEmpty()

-- #29 Collection Size Constraint in SensorNode
-- Difficulty: medium
context SensorNode
inv: self.metrics->size() > 1

-- #30 Set Difference in Cluster
-- Difficulty: medium
context Cluster
inv: self.edgeNodes->excludingAll(self.edgeNodes)->isEmpty()

-- #31 Collection Maximum Size in IoTPlatform
-- Difficulty: medium
context IoTPlatform
inv: self.accessPolicies->size() <= 3

-- #32 Collection As Set Conversion in SensorNode
-- Difficulty: hard
context SensorNode
inv: self.metrics->asSet()->size() <= self.metrics->size()

-- #33 SortedBy Operation in Device
-- Difficulty: expert
context Device
inv: self.tickets->notEmpty() implies self.tickets->sortedBy(x | x.ticketId)->first().ticketId <= self.tickets->sortedBy(x | x.ticketId)->last().ticketId

-- #34 Collection At Index in Firmware
-- Difficulty: expert
context Firmware
inv: self.devices->size() >= 4 implies self.devices->asSequence()->at(4).deviceId <> null

-- #35 IncludesAll/ExcludesAll Check in Device
-- Difficulty: medium
context Device
inv: self.dataStreams->includesAll(self.dataStreams) or self.dataStreams->excludesAll(self.dataStreams)

-- #36 String Not Empty in Location
-- Difficulty: medium
context Location
inv: self.site <> '' and self.site.size() > 0

-- #37 Collection As Set Conversion in Cluster
-- Difficulty: expert
context Cluster
inv: self.edgeNodes->asSet()->size() <= self.edgeNodes->size()

-- #38 Collection Not Empty Check in DataStream
-- Difficulty: medium
context DataStream
inv: self.alertRules->size() > 0

-- #39 All Different in Device
-- Difficulty: expert
context Device
inv: self.dataStreams->forAll(x, y | x <> y implies x.topic <> y.topic)

-- #40 Set Difference in IoTPlatform
-- Difficulty: medium
context IoTPlatform
inv: self.networks->excludingAll(self.networks)->isEmpty()

-- #41 Collection Must Be Empty in User
-- Difficulty: easy
context User
inv: self.apiKeys->isEmpty()

-- #42 Collection Last Element in IoTPlatform
-- Difficulty: hard
context IoTPlatform
inv: self.networks->notEmpty() implies self.networks->last().networkId <> null

-- #43 String Minimum Length in EventLog
-- Difficulty: medium
context EventLog
inv: self.message.size() >= 12

-- #44 String Not Empty in APIKey
-- Difficulty: medium
context APIKey
inv: self.value <> '' and self.value.size() > 0

-- #45 Collection Not Empty (Simple) in IoTPlatform
-- Difficulty: easy
context IoTPlatform
inv: self.accessPolicies->notEmpty()

-- #46 String Contains Substring in Network
-- Difficulty: medium
context Network
inv: self.name.indexOf('value') >= 0

-- #47 String Maximum Length in OTAUpdate
-- Difficulty: medium
context OTAUpdate
inv: self.scheduledAt.size() <= 44

-- #48 SortedBy Operation in Role
-- Difficulty: expert
context Role
inv: self.policies->notEmpty() implies self.policies->sortedBy(x | x.rulesJson)->first().rulesJson <= self.policies->sortedBy(x | x.rulesJson)->last().rulesJson

-- #49 Collection As Set Conversion in Device
-- Difficulty: hard
context Device
inv: self.eventLogs->asSet()->size() <= self.eventLogs->size()

-- #50 Collection Sum in Gateway
-- Difficulty: hard
context Gateway
inv: self.connectedEdges->collect(x | x.memoryMB)->sum() >= 6

-- #51 Collection Minimum Size in DataStream
-- Difficulty: medium
context DataStream
inv: self.alertRules->size() >= 2

-- #52 String Contains Substring in User
-- Difficulty: medium
context User
inv: self.email.indexOf('value') >= 0

-- #53 Collection As Set Conversion in Gateway
-- Difficulty: hard
context Gateway
inv: self.connectedEdges->asSet()->size() <= self.connectedEdges->size()

-- #54 String Exact Length in DataStream
-- Difficulty: easy
context DataStream
inv: self.streamId.size() = 21

-- #55 Collection Last Element in AccessPolicy
-- Difficulty: hard
context AccessPolicy
inv: self.roles->notEmpty() implies self.roles->last().roleName <> null

-- #56 Set Union in IoTPlatform
-- Difficulty: medium
context IoTPlatform
inv: self.roles->union(self.roles)->notEmpty()

-- #57 Set Difference in IoTPlatform
-- Difficulty: medium
context IoTPlatform
inv: self.roles->excludingAll(self.roles)->isEmpty()

-- #58 Attribute Must Be Null in Device
-- Difficulty: trivial
context Device
inv: self.status = null

-- #59 String Maximum Length in IoTPlatform
-- Difficulty: medium
context IoTPlatform
inv: self.version.size() <= 21

-- #60 Set Union in ConnectivityModule
-- Difficulty: medium
context ConnectivityModule
inv: self.links->union(self.links)->notEmpty()

-- #61 Attribute Must Be Null in User
-- Difficulty: trivial
context User
inv: self.name = null

-- #62 Numeric Greater Than Value in PowerSource
-- Difficulty: trivial
context PowerSource
inv: self.capacity > 32

-- #63 Collection Not Empty Check in Firmware
-- Difficulty: medium
context Firmware
inv: self.devices->size() > 0

-- #64 String Minimum Length in DataStream
-- Difficulty: medium
context DataStream
inv: self.topic.size() >= 15

-- #65 Flatten Operation in Role
-- Difficulty: expert
context Role
inv: self.policies->collect(x | x.roles)->flatten()->notEmpty()

-- #66 Attribute Must Be Null in User
-- Difficulty: trivial
context User
inv: self.userId = null

