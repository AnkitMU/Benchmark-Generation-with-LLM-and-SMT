-- Benchmark: IOTsensornetwork / balanced
-- Type: UNSAT Constraints Only
-- Generated: 2026-03-17T21:04:11.565153
-- Count: 18

-- #1 Collection Maximum Size (UNSAT) in IoTPlatform
-- Difficulty: medium
-- Mutation Strategy: contradictory_bounds
context IoTPlatform
inv: (self.networks->size() <= 1) and (self.networks->size() > 1)

-- #2 Attribute Not Null (Simple) (UNSAT) in ActuatorNode
-- Difficulty: easy
-- Mutation Strategy: simple_negation
context ActuatorNode
inv: not(self.actuatorType <> null)

-- #3 Association Exists (UNSAT) in OTAUpdate
-- Difficulty: easy
-- Mutation Strategy: simple_negation
context OTAUpdate
inv: not(self.targetDevice <> null)

-- #4 Association Exists (UNSAT) in CommunicationLink
-- Difficulty: easy
-- Mutation Strategy: simple_negation
context CommunicationLink
inv: not(self.stream <> null)

-- #5 Association Exists (UNSAT) in Network
-- Difficulty: easy
-- Mutation Strategy: simple_negation
context Network
inv: not(self.platform <> null)

-- #6 Collection Size in Range (UNSAT) in Firmware
-- Difficulty: medium
-- Mutation Strategy: contradictory_bounds
context Firmware
inv: (self.devices->size() >= 1 and self.devices->size() <= 1) and (self.devices->size() < 1)

-- #7 String Maximum Length (UNSAT) in AlertRule
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context AlertRule
inv: not(self.ruleId.size() <= 41)

-- #8 SortedBy Operation (UNSAT) in AccessPolicy
-- Difficulty: hard
-- Mutation Strategy: empty_collection
context AccessPolicy
inv: (self.roles->notEmpty() implies self.roles->sortedBy(x | x.roleName)->first().roleName <= self.roles->sortedBy(x | x.roleName)->last().roleName) and (self.roles->isEmpty())

-- #9 Two Attributes Not Equal (UNSAT) in Device
-- Difficulty: easy
-- Mutation Strategy: simple_negation
context Device
inv: not(self.deviceId <> self.name)

-- #10 SortedBy Operation (UNSAT) in DataStream
-- Difficulty: hard
-- Mutation Strategy: empty_collection
context DataStream
inv: (self.alertRules->notEmpty() implies self.alertRules->sortedBy(x | x.condition)->first().condition <= self.alertRules->sortedBy(x | x.condition)->last().condition) and (self.alertRules->isEmpty())

-- #11 Collection As Set Conversion (UNSAT) in SensorNode
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context SensorNode
inv: not(self.metrics->asSet()->size() <= self.metrics->size())

-- #12 IncludesAll/ExcludesAll Check (UNSAT) in DataStream
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context DataStream
inv: not(self.records->includesAll(self.records) or self.records->excludesAll(self.records))

-- #13 Collection As Set Conversion (UNSAT) in Cluster
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context Cluster
inv: not(self.edgeNodes->asSet()->size() <= self.edgeNodes->size())

-- #14 Two Attributes Not Equal (UNSAT) in Alert
-- Difficulty: easy
-- Mutation Strategy: simple_negation
context Alert
inv: not(self.alertId <> self.message)

-- #15 String Not Empty (UNSAT) in AccessPolicy
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context AccessPolicy
inv: not(self.description <> '' and self.description.size() > 0)

-- #16 Collection As Set Conversion (UNSAT) in Device
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context Device
inv: not(self.eventLogs->asSet()->size() <= self.eventLogs->size())

-- #17 Collection Last Element (UNSAT) in AccessPolicy
-- Difficulty: medium
-- Mutation Strategy: empty_collection
context AccessPolicy
inv: (self.roles->notEmpty() implies self.roles->last().roleName <> null) and (self.roles->isEmpty())

-- #18 All Different (UNSAT) in User
-- Difficulty: medium
-- Mutation Strategy: simple_negation
context User
inv: not(self.roles->forAll(x, y | x <> y implies x.roleName <> y.roleName))

